name: Tests du moteur

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  moteur:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node test/moteur.js
